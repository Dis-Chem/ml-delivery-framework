# UAT environment overrides — only the catalog changes; dataset shape stays defined in
# conf/base/catalog.yml.
training_data:
  type: databricks.ManagedTableDataset
  catalog: {{ .input_staging_catalog_name }}
  schema: {{ .input_schema_name }}
  table: training_data
